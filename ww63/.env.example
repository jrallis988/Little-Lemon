# Copy to .env.local for local dev, or set in Vercel/Netlify project settings.
# Used for canonical URLs and Open Graph image absolute paths in production.
VITE_SITE_URL=https://your-deployment-url.vercel.app
