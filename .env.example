# Forward Contact, Volunteer, Town, and Join submissions to a webhook
# (Zapier, Make, Google Apps Script, NationBuilder, etc.)
FORM_WEBHOOK_URL=
# Legacy alias still honored:
JOIN_WEBHOOK_URL=

# Email campaign staff via Resend when someone submits a form
RESEND_API_KEY=
FORM_NOTIFY_TO=vargaforsenate@gmail.com
FORM_FROM_EMAIL=onboarding@resend.dev
# Legacy aliases still honored:
JOIN_NOTIFY_TO=vargaforsenate@gmail.com
JOIN_FROM_EMAIL=onboarding@resend.dev
