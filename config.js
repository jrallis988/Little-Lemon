/* Playhouse site config — edit these before going live */
window.PLAYHOUSE_CONFIG = {
  // Formspree: create a form at https://formspree.io and paste the endpoint, e.g.
  // "https://formspree.io/f/xxxxxxxx"
  // Leave empty to keep the local success UI without sending email.
  formEndpoint: "",

  // Optional: used for absolute social preview URLs once you have a domain
  // e.g. "https://playhouseanimation.com"
  siteUrl: "",

  // Optional default YouTube/Vimeo IDs (buttons can still override via data attributes)
  showreelYoutube: "",
  showreelVimeo: ""
};
