[build]
  command = "mkdir -p dist && cp -r *.html *.css *.js *.svg *.txt *.xml *.md images dist/ && cp package.json dist/"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
